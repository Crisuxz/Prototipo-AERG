# Base de la API. Con el proxy de Vite en desarrollo basta con /api.
VITE_API_BASE_URL=/api
# Token de sesion del prototipo; debe coincidir con AUTH_TOKEN del backend.
VITE_API_TOKEN=prototipo-dev-token
